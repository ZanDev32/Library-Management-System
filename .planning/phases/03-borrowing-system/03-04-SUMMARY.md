# Phase 3-04: Frontend Student UI

## Tasks Completed
- [x] Create Borrow Confirmation Modal (`BorrowModal.jsx`)
- [x] Update Catalog UI (`BookCard.jsx`)
- [x] Implement QR Code Generator Component (`QRCodeDisplay.jsx`)
- [x] Create "My Loans" Dashboard (`MyLoans.jsx`)

## Notes
- `BorrowModal` handles the 2-step confirmation and dynamic waitlist text.
- `BookCard` handles showing availability and the correct button (Pinjam/Masuk Antrian).
- `QRCodeDisplay` generates a QR code using a public API.
- `MyLoans` dashboard fetches and groups loans into Active, Pending, and History sections, and allows triggering the QR modal and extension requests.
