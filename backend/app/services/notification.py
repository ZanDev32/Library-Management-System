def send_status_update_notification(user_id: int, status: str, message: str):
    # Stub for sending notifications (email/in-app)
    print(f"[NOTIFICATION] User {user_id}: Status is now {status}. Message: {message}")
